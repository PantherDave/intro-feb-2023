version: "3"
services:
  db:
    image: mcr.microsoft.com/mssql/server
    volumes:
      - sql-data:/var/opt/mssql/data
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=TokyoJoe138!
    ports:
      - 1433:1433

volumes:
  sql-data: